# Project Image Notes

## Requirements
- **Aspect ratio**: 16:9
- **Title**: PixTrap
- **Subtitle**: Brazilian Portuguese Pix fraud safety benchmark

## Primary Visual
- Use the real `calibration_scatter.png` chart from `results/charts/` if evaluation results are ready.
- The chart should be clearly readable at thumbnail size.

## Fallback Visual
- If results are not yet ready, use a polished PixTrap taxonomy diagram or benchmark pipeline diagram showing:
  - The four fraud categories.
  - The benign near-neighbor pairing concept.
  - The model evaluation flow.

## Constraints
- **No raw scam prompt text** in the image.
- **No decorative AI art** as the primary image if a real chart or real methodology figure exists.
- Generative imagery is allowed only as a secondary promotional asset, not the main evidence-bearing submission image.
- Must be readable at thumbnail size (clear labels, sufficient contrast, no tiny fonts).

## Production Notes
- Export the calibration scatter chart at 300 DPI with a 16:9 crop.
- Add a title bar with "PixTrap" and the subtitle overlaid on a clean background.
- Save final image to `submission/project_image/`.
